rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Временно разрешаем всем (для тестирования)
    // TODO: Добавить Firebase Authentication для безопасности
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}

